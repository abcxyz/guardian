name: 'Guardian Unlock - Test'
run-name: 'Guardian Unlock - PR #${{ github.event.issue.number }}'

on:
  issue_comment:
    types:
      - 'created'

concurrency:
  group: '${{ github.workflow }}-${{ github.head_ref || github.ref }}'

permissions:
  contents: 'read'
  id-token: 'write'
  pull-requests: 'write'
  checks: 'write'

jobs:
  unlock:
    uses: 'abcxyz/guardian/.github/workflows/unlock.yml@main' # ratchet:exclude
    with:
      wif_provider: '${{ vars.GUARDIAN_WIF_PROVIDER}}'
      wif_service_account: '${{ vars.GUARDIAN_WIF_SERVICE_ACCOUNT }}'
      guardian_bucket_name: '${{ vars.GUARDIAN_BUCKET_NAME }}'
