name: 'Guardian Unlock - Test'
run-name: 'Guardian Unlock - PR #${{ github.event.issue.number }}'

on:
  issue_comment:
    types:
      - 'created'

concurrency:
  group: '${{ github.workflow }}-${{ github.head_ref || github.ref }}'

permissions:
  contents: 'read'
  id-token: 'write'
  pull-requests: 'write'
  checks: 'write'

jobs:
  unlock:
    uses: 'abcxyz/warden/.github/workflows/unlock.yml@main' # ratchet:exclude
    with:
      wif_provider: 'projects/200062081032/locations/global/workloadIdentityPools/github-pool/providers/github-provider-abcxyz-warden'
      wif_service_account: 'warden-ci-sa@verbanicm-dev.iam.gserviceaccount.com'
      guardian_bucket_name: 'verbanicm-dev-terraform'
      guardian_tf_version: '1.1.8'
